const { createTheme } = require('@mui/material');

export const theme = createTheme({
  palette: {
    primary: {
      main: '#471CA9',
    },
    secondary: {
      main: '#5CD3A8',
    },
  },
});
